# Optional: Supabase configuration for Vercel / production deployment
# If not provided, the portfolio automatically operates using browser local storage with zero configuration.
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
